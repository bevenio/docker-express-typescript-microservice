/**
 * The goal of this base options for schemas is ensuring that all saved models have the same base format
 * and follow the same rules for consistency.
 **/

export const BaseSchemaOptions = {
  timestamps: true,
}
